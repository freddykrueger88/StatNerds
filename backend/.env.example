# Datenbank
DATABASE_URL=postgresql://statnerds:statnerds@db:5432/statnerds

# CORS – kommagetrennte erlaubte Origins (Standard: localhost:3000)
ALLOWED_ORIGINS=http://localhost:3000

# Admin-API-Key für sensitive Endpunkte (z.B. /stats/cleanup)
# Bitte einen sicheren zufälligen Wert setzen!
ADMIN_API_KEY=bitte-aendern-sicherer-zufalls-wert

# Optional: API-Football (100 Req/Tag kostenlos)
API_FOOTBALL_KEY=dein_key_hier
